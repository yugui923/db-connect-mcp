[project]
name = "pg-da"
version = "0.1.0"
description = "postgres data analyst toolkit MCP"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "psycopg[binary]>=3.1.0",
    "pandas>=2.0.0",
    "python-dotenv>=1.0.0",
]
