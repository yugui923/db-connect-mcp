{
  "mcpServers": {
    "db-connect-mcp-pg": {
      "command": "uv",
      "args": ["--directory", "C:/Code/db-connect-mcp", "run", "main.py"],
      "env": {
        "DATABASE_URL": "postgresql://username:password@localhost:5432/your_database"
      }
    }
  }
}
